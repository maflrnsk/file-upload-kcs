{
  "name": "bookstack-sync",
  "version": "1.0.0",
  "description": "Sync GitHub Issues to BookStack",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "marked": "^9.1.0"
  }
}
